name: macos

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

env:
  IRRLICHT_TAG: 1.9.0mt1

jobs:
  build:
    runs-on: macos-10.15

    steps:
      - uses: actions/checkout@v2

      # - uses: Homebrew/actions/setup-homebrew@master

      - name: Install deps
        run: |
          source ./util/ci/common.sh
          #install_macos_deps
          wget "https://github.com/FnControlOption/irrlicht/releases/download/1.9.0mt3-macos/macos-catalina.tar.gz"
          gtar -xaf macos-catalina.tar.gz -C /usr/local --touch
          #brew install cmake libpng jpeg sqlite leveldb libogg libvorbis freetype gmp jsoncpp
          #brew reinstall libpng
          brew unlink $(brew ls --formula)
          pkgs=(cmake freetype gettext jpeg libogg libvorbis)
          brew link ${pkgs[@]}

#       - name: Build
#         run: |
#           ./util/ci/build.sh

#       - name: Test
#         run: |
#           ./bin/minetest --run-unittests

      - name: Build
        run: |
          #git clone https://github.com/minetest/irrlicht -b $IRRLICHT_TAG
          #cd irrlicht
          #cmake . -DBUILD_SHARED_LIBS=OFF
          #make -j2
          #cd ..
          mkdir cmakebuild
          mkdir -p build/macos/
          cd cmakebuild
          #  -DENABLE_FREETYPE=TRUE -DENABLE_GETTEXT=TRUE \
          #  -DIRRLICHT_LIBRARY=$PWD/../irrlicht/lib/OSX/libIrrlichtMt.a \
          #  -DIRRLICHT_INCLUDE_DIR=$PWD/../irrlicht/include \
          #  -DGETTEXT_MSGFMT=/usr/local/opt/gettext/bin/msgfmt \
          #  -DGETTEXT_INCLUDE_DIR=/usr/local/opt/gettext/include \
          #  -DGETTEXT_LIBRARY=/usr/local/opt/gettext/lib/libintl.a \
          #  -DJPEG_INCLUDE_DIR=/usr/local/opt/jpeg/include \
          #  -DJPEG_LIBRARY=/usr/local/opt/jpeg/lib/libjpeg.a \
          #  -DPNG_INCLUDE_DIR=/usr/local/opt/libpng/include \
          #  -DPNG_LIBRARY=/usr/local/opt/libpng/lib/libpng.a \
          cmake -DCMAKE_OSX_DEPLOYMENT_TARGET=10.14 \
            -DCMAKE_INSTALL_PREFIX=../build/macos/ \
            -DCMAKE_BUILD_TYPE=Release -DRUN_IN_PLACE=FALSE \
            -DBUILD_CLIENT=TRUE -DBUILD_SERVER=FALSE ..
          make -j2
          make install

          # make package
          # unzip minetest-*-osx.zip
          # mv minetest.app ..

      - uses: actions/upload-artifact@v2
        with:
          name: minetest-macos
          path: ./build/macos/

#       - uses: actions/upload-artifact@v2
#         with:
#           name: minetest-macos-app
#           path: ./minetest.app
      
